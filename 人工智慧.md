- 第一章：[[回歸分析]](Regression)
	- Regression_python實作範例 (回歸實作與評估)

回歸模型的評估指標、常見問題（如欠擬合/過擬合）及 Python 程式實作。

- **評估指標**：詳細說明 MAE（平均絕對誤差）、MSE（平均平方誤差）及 RMSE（均方根誤差）的公式與應用情境（如對極端值的敏感度）。
- **偏差與方差 (Bias & Variance)**：透過射箭圖示解釋模型準確性與穩定性的權衡關係。
- **Python 實作流程**：使用 `scikit-learn` 套件進行簡單線性、多元線性及多項式回歸的程式撰寫步驟。
- **資料預處理**：比較正規化 (Normalization) 與標準化 (Standardization) 的目的與公式。
- **模型擬合問題**：探討欠擬合 (Underfitting) 與過擬合 (Overfitting) 的成因與解決對策。
- **優化算法**：介紹梯度下降法 (Gradient Descent) 的原理、學習率 (Learning Rate) 的設定，以及區域/全域最小值問題。

### 第二章：Train_Test_Split (資料分割與驗證)

本章介紹如何建立 AI 模型的標準流程，以及如何科學地分割與驗證資料。

- **模型建立五步驟**：匯入套件、資料清理、建立模型、訓練模型、模型評估。
- **資料分割方法**：
    - **隨機分割**：適用於一般獨立數據。
    - **分層分割**：確保訓練集與測試集的類別比例一致，適用於不平衡數據。
    - **時間序列分割**：遵循「用過去預測未來」原則，防止未來資訊洩漏。
- **K-fold 交叉驗證**：解釋如何透過 K 次輪流考驗來取得客觀的效能報告，並區分用於「模型評估」與「模型選擇」的不同流程。

**AI04_Classification_LogisticRegression (分類與邏輯回歸)**

本章轉向分類任務，介紹邏輯回歸的原理與分類效能指標。

- **分類 vs. 回歸**：區分預測連續數值（回歸）與預測類別（分類）的差異。
- **邏輯回歸原理**：利用線性模型結合 Sigmoid 函數，將輸出限制在 0 到 1 之間以代表機率。
- **資料特性要求**：探討線性可分、大致線性可分與非線性可分資料對模型的影響。
- **混淆矩陣與指標**：
    - **四大指標**：準確率 (Accuracy)、精確度 (Precision)、召回率 (Recall/Sensitivity)、特異度 (Specificity)。
    - **F1 Score**：精確度與召回率的調和平均數。
    - **不平衡資料陷阱**：解釋為何在高偏態數據下，單看準確率會產生誤導（如醫療 AI 預測所有人健康卻錯失病人）。
- **實作範例**：以 Python 預測感冒發生為例，展示特徵處理、模型訓練與混淆矩陣熱點圖的繪製