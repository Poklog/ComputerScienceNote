回歸分析探討**依變項 (Y)**與一個或多個**自變項 (X)**之間關係的統計方法。以下針對其理論架構、數學模型、統計檢定及變數選擇方法進行詳細分析：

### 1.定義與用途

回歸的基本邏輯是建立函數，用來解釋或預測數據。其主要應用包含：

- **預測趨勢**：如預測未來氣溫、股價或房價。
- **檢測變項關係**：探索變數間（如溫度與降雨機率）是否存在關連。
- **輔助決策**：確定哪些變項對結果影響最顯著，並預測決策後的後果。

### 2. ==線性回歸== (Linear Regression)

線性回歸假設 Y 與 X 之間存在直線關係，其標準公式為： 
$$Y=P_0​+P_1​X+ϵ$$

- **截距 ($P_0$​ 或 $a$)**：當 X=0 時的 Y 預測值。
- **斜率 ($P_1$​ 或 $b$)**：當 X 變動一單位時，Y 的平均變動量。
- **隨機誤差 ($ϵ$)**：代表實際觀察值與模型預測值之間的落差，即模型無法解釋的部分。

回歸分析在尋找「最佳擬合線」時有兩種不同的數學假設：

- **[[OLS]] (普通最小平方法)**
- **TLS (總體最小平方法)**

### 3. 統計顯著性與假設檢定

為了判斷自變項 X 是否真的對 Y 有影響，必須觀察 **p-value (p值)**：

- **虛無假設 ($H_0$)**：X與Y之間==沒有顯著==差異或效果（$p≥0.05$）。
- **對立假設 ($H_1$)**：X與Y之間有==差異==（$p<0.05$）。
- **顯著程度**：$p<0.05$ 代表顯著，$p<0.01$ 為很顯著，$p<0.001$ 則是非常顯著。

### 4. 多元回歸與變數篩選 (Multiple & Stepwise Regression)

當存在多個自變項時，模型演變為**多元回歸**：
$$Y=a+b_1​X_1​+b_2​X_2​+...+ϵ$$ 
處理大量變數時，常使用 **逐步回歸 (Stepwise Regression)** 來篩選重要特徵：

- **向前選擇 (Forward Selection)**：從無變數的模型開始，逐一加入對模型解釋力貢獻最顯著的變數。
- **==向後刪除== (Backward Elimination)**：從包含所有變數的完整模型開始，==逐一剔除== p值最高（最不顯著）的變數，直到所有留下的變數都符合顯著性標準為止。


### 5. 模型效能評估指標

回歸模型的好壞通常由誤差量化，常見指標包括：

- **[[MAE]] (平均絕對誤差)**
- **[[MSE]] (平均平方誤差)**：
- **[[RMSE]] (均方根誤差)**