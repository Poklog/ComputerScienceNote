混淆矩陣（Confusion Matrix）是機器學習中用來評估分類模型性能的一種標準化工具，它以表格的形式呈現模型預測結果與真實標籤之間的對應關係。


### 基本結構
混淆矩陣通常以二元分類為例（正向 Positive 與負向 Negative），將結果分為四個類別：

- **真正向 (True Positive, TP)**：實際為正向，模型也正確預測為正向。
- **偽正向 (False Positive, FP)**：實際為負向，模型卻錯誤預測為正向（俗稱「誤報」或「誤診」）。
- **偽負向 (False Negative, FN)**：實際為正向，模型卻錯誤預測為負向（俗稱「漏報」或「漏診」）。
- **真負向 (True Negative, TN)**：實際為負向，模型也正確預測為負向。

|          | 實際正向     | 實際負向     |
| -------- | -------- | -------- |
| **預測正向** | 真正向 (TP) | 偽負向 (FN) |
| **預測負向** | 偽負向 (FN) | 真負向 (TN) |


### 基於混淆矩陣的評估指標

從這四個數值中，可以計算出多項關鍵指標來衡量模型的表現：

- **準確率 (Accuracy)**：整體預測正確的比例。公式：(TP+TN)/(TP+FP+TN+FN)。
- **精確度 (Precision)**：在所有被預測為「正向」的人中，真正生病的比例有多少？這代表「抓得準不準」，適用於不能亂抓人的情境（如垃圾郵件過濾）。
- **召回率 (Recall / 敏感性)**：在所有真正生病的人中，成功被模型抓出來的比例。這代表「有沒有漏網之魚」，在醫療診斷或地震警報中至關重要。
- **特異度 (Specificity)**：在所有真正健康的人中，成功被判定為健康的比例，衡量的是對負樣本的辨識能力。
- **F1 分數 (F1 Score)**：精確度與召回率的調和平均值，用來平衡這兩個可能互相衝突的指標。

### 為什麼需要混淆矩陣？

只看「準確率」往往會產生誤導，特別是在**資料不平衡（Imbalanced Data）**的情況下。

- **案例**：假設 10,000 人中只有 10 人生病（盛行率 0.1%），如果模型偷懶直接預測所有人「都健康」，其準確率仍高達 99.9%，但它完全沒抓出病人，在醫療上是失敗的。此時透過混淆矩陣觀察 **Recall** 是否為 0，才能發現真實問題。

### 多類別分類的應用

混淆矩陣也可以擴展到多類別分類。例如在 **Iris（鳶尾花）資料集**的分類任務中，矩陣會變成 3x3 的形式，分別代表 Setosa、Versicolor 與 Virginica 三種花色，用以觀察模型在不同類別間是否容易產生混淆。