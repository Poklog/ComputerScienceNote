---
tags:
  - 節點
---
**資料預處理（Preprocessing）** 如果特徵（如「面積」與「房間數」）數值範圍差異太大，或是避免溢位，模型可能會產生偏差或計算不穩定。

在計算 OLS 或 TLS 時，經常涉及矩陣乘法（如 $X^TX$）。如果原始數據很大（例如 $10^6$），其平方會變成 $10^{12}$，這在複雜運算中極易導致**浮點數溢位**或精度遺失。

- **方法：**
	- [[標準化]]
	- [[正規化]]