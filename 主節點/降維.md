降維 (Dimensionality Reduction)

### 降維的目的

- 資料可視化：將原本無法想像的高維空間（如 4 維或 100 維）壓低至 2 維或 3 維，讓人類能直觀觀察資料的群聚 (Clustering) 結構與異常點。
- 提升效能：去除雜訊與重複資訊（共線性），可以減少記憶體消耗並加速模型訓練。
- 避免維度詛咒 (Curse of Dimensionality)：當維度增加時，資料會變得極其稀疏且所需的樣本量呈指數級爆炸，這會導致距離運算失效並引起過擬合 (Overfitting)。

- 作為特徵工程(SVD)的一部分

### 降維分類
分為
- 線性降維
	- [[PCA]]
	- LDA
- 非線性降維
	- t-SNE
	- UMAP
	- Autoencoder

3. PCA 與 LDA 的差異
    無監督學習：PCA 是一種無監督 (Unsupervised) 技術，它在降維時不參考類別標籤，純粹觀察特徵間的分布與變異。
    比較：相較之下，LDA (線性判別分析) 是監督式學習，其目標是「讓同類擠在一起，不同類拉開」，側重於類別分離而非單純的變異保留。
### 特徵工程